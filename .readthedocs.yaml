version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py

jobs:
  build:
      # Ensure FORD-generated HTML files are included in the output
      output_base: docs
