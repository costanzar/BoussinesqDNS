version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

python:
  install:
    - method: pip
      path: .
jobs:
  pre_build:
    - pip install ford
  build:
    - ford Documentation.md -o docs  # Build documentation on RTD
sphinx:
  builder: dirhtml